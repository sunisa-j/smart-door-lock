'use strict';

window.app.controller('DoorsApprovalController', function ($scope) {

    // Set Default Menu (Menu contains are 'waitingApprove' and 'approved')
    $scope.doorApproveMenu = {
        name: 'waitingApprove'
    };

});
